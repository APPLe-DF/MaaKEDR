name: Schema Sync

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * *"

permissions:
  contents: write

jobs:
  sync:
    if: github.event.repository.fork == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: node tools/sync-schema.mjs
      - run: pnpm install --frozen-lockfile
      - run: pnpm check:schema
      - name: Install MaaFW runtime system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libpipewire-0.3-0t64 || sudo apt-get install -y libpipewire-0.3-0
      - run: pnpm exec maa-tools check
      - name: Commit schema updates
        run: |
          if git diff --quiet; then
            echo "No schema changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          node tools/update-lock-baselines.mjs
          git add tools/schema maa-project.lock.json
          git commit -m "chore: sync MaaFW schema [skip changelog]"
          git push
